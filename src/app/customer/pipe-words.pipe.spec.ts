import { PipeWordsPipe } from './pipe-words.pipe';

describe('PipeWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
